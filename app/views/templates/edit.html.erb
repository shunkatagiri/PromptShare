<!-- app/views/templates/edit.html.erb -->

<section class="bg-white">
  <div class="flex flex-col items-center justify-center px-6 py-8 mx-auto md:h-screen lg:py-0">
    <h2 class="mb-4 text-center text-2xl font-bold text-gray-800 md:mb-8 lg:text-3xl">Edit Template</h2>
    <div class="w-full bg-white rounded-lg shadow md:mt-0 sm:max-w-xl xl:p-0">
      <div class="p-6 space-y-4 md:space-y-6 sm:p-8">
        <%= form_with(model: @template, local: true, method: :patch) do |form| %>
          <!-- 省略：フォームの中身は new.html.erb と同じ -->
        <% end %>  
      </div>
    </div>
  </div>
</section>
