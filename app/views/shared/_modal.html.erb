<!-- app/views/shared/_modal.html.erb -->

<div data-modal-target="overlay" class="fixed inset-0 bg-black bg-opacity-50 hidden" data-action="click->modal#close"></div>
<div data-modal-target="content" class="fixed inset-0 flex items-center justify-center hidden">
  <%= render 'shared/modal_content' %>
</div>
